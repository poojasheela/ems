spring:
  cloud:
    vault:
      enabled: false

  data:
    mongodb:
      uri: mongodb://localhost:27017/test

github:
  token: dummy-token
  uri: https://api.github.com
  owner: dummy-owner
  repo: dummy-repo
  workflowFileName: dummy.yml
